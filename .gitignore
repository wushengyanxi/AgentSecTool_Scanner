# --- 凭据与本地配置（绝不入库）---
config/scanner.yaml
config/blocklist.txt
fofa/fofa.ini
geoip/geoip.ini

# --- GeoIP 库文件（体积大，第三方分发，本地下载，不入库）---
*.mmdb
geoip/GeoLite2-City_*/

# --- 扫描产物 ---
*.jsonl
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
candidates.csv
results*.json
scope/cn-cidrs.txt

# --- Go 构建产物 ---
prober/bin/
prober/zgrab2-openclaw

# --- Python ---
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/

# --- 指纹库本地覆盖 ---
fingerprints/*.local.json

# --- 系统 ---
.DS_Store

# 指纹采集临时报告（每次运行变化）
fingerprint/collect_corpus_report.json

# 拆库后的数据（真实暴露主机，绝不入库）
data/

# 测试/调试样本（含真实暴露主机清单与探测结果，绝不入库）
scan_test/

# 前端模板（仅本地使用，不入库）
front_end_template/
