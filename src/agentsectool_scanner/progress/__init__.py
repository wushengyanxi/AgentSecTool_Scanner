"""扫描进度的块级状态：把"待扫描范围"切成 CIDR 块，按"扫描轮次(campaign)"登记，
可原子领取、标记完成、查询进度。支持跨多次运行续扫；同一共享 DB 下支持多进程协作。

设计要点（区别于"逐 IP 记录"）：
  - 单位是 CIDR 块，不是单个 IP——中国大陆约 3.4 亿地址，逐 IP 记录不现实。
  - 按扫描轮次组织——暴露面随时间变化，需定期重扫，而非"扫过一次永不再扫"。
  - 跨设备：同一 SQLite 文件不会自动跨设备同步；要么用分片（masscan/ZMap --shard，
    各设备扫互斥子集，无需共享状态），要么把各设备指向同一个共享 DB。
"""
